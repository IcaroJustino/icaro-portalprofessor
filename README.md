# Portal do Professor - Local Setup

## Pré-requisitos

- Node.js >= 20
- NPM >= 9
- Java 17+
- Maven 3+
- Docker e Docker Compose

---

## Rodando com Docker Compose

1. No diretório raiz (onde está o `docker-compose.yml`), execute:

```bash
docker-compose up --build
